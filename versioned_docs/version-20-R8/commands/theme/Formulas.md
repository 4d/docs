---
id: Formulas
title: Formulas
---
||
|---|
|[**EDIT FORMULA** ( *aTable* ; *formula* )](../../commands-legacy/edit-formula)<br/>The **EDIT FORMULA** command displays the Formula editor in order to let the user write or modify a formula.|
|[**EXECUTE FORMULA** ( *statement* )](../../commands-legacy/execute-formula)<br/>**EXECUTE FORMULA** executes *statement* as a line of code.|
|[**Formula** ( *formulaExp* : Expression ) : 4D.Function](../../commands/formula)<br/>creates a `4D Function` object based upon the *formulaExp* expression|
|[**Formula from string**( *formulaString* : Text ) : 4D.Function<br/>**Formula from string**( *formulaString* : Text ; *context* : Longint ) : 4D.Function](../../commands/formula-from-string)<br/>creates a `4D.Function` object based upon the *formulaString* and, optionnally, a *context*|
|[**GET ALLOWED METHODS** ( *methodsArray* )](../../commands-legacy/get-allowed-methods)<br/>The **GET ALLOWED METHODS** command returns, in *methodsArray*, the names of methods that can be used to write formulas.|
|[**Parse formula** ( *formula* {; *options*}{; *errorMessage*} ) : Text](../../commands-legacy/parse-formula)<br/>The **Parse formula** function analyzes the 4D *formula*, checks its syntax, and returns its normalized form.|
|[**SET ALLOWED METHODS** ( *methodsArray* )](../../commands/set-allowed-methods)<br/>The **SET ALLOWED METHODS** command designates the project methods that can be entered via the application.|
