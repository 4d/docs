---
id: Arrays
title: Arrays
---
||
|---|
|[**APPEND TO ARRAY** ( *array* ; *value* )](../../commands-legacy/append-to-array)<br/>The APPEND TO ARRAY command adds a new element at the end of *array* and assigns *value* to the element.|
|[**ARRAY BLOB** ( *arrayName* ; *size* {; *size2*} )](../../commands-legacy/array-blob)<br/>The **ARRAY BLOB** command creates and/or resizes an array of Blob type elements in memory.|
|[**ARRAY BOOLEAN** ( *arrayName* ; *size* {; *size2*} )](../../commands-legacy/array-boolean)<br/>The ARRAY BOOLEAN command creates and/or resizes an array of [Boolean](# "Can be either TRUE or FALSE") elements in memory.|
|[**ARRAY DATE** ( *arrayName* ; *size* {; *size2*} )](../../commands-legacy/array-date)<br/>The ARRAY DATE command creates and/or resizes an array of [Date](# "In the range of 1/1/100 to 12/31/32,767") elements in memory.|
|[**ARRAY INTEGER** ( *arrayName* ; *size* {; *size2*} )](../../commands-legacy/array-integer)<br/>The ARRAY INTEGER command creates and/or resizes an array of 2-byte [Integer](# "Number between -32,768..32,767 (2^15..(2^15)-1) (2-byte integer)") elements in memory.|
|[**ARRAY LONGINT** ( *arrayName* ; *size* {; *size2*} )](../../commands-legacy/array-longint)<br/>The ARRAY LONGINT command creates and/or resizes an array of 4-byte [Longint](# "Number between -2^31..(2^31)-1 (4-byte Integer)") elements in memory.|
|[**ARRAY OBJECT** ( *arrayName* ; *size* {; *size2*} )](../../commands-legacy/array-object)<br/>The **ARRAY OBJECT** command creates and/or resizes an array of language Object type elements in memory.|
|[**ARRAY PICTURE** ( *arrayName* ; *size* {; *size2*} )](../../commands-legacy/array-picture)<br/>The ARRAY PICTURE command creates and/or resizes an array of [Picture](# "Can be any Windows or Macintosh picture") elements in memory.|
|[**ARRAY POINTER** ( *arrayName* ; *size* {; *size2*} )](../../commands-legacy/array-pointer)<br/>The **ARRAY POINTER** command creates or resizes an array of [Pointer](# "A reference to another variable (including arrays and array elements), table, or field") elements in memory.|
|[**ARRAY REAL** ( *arrayName* ; *size* {; *size2*} )](../../commands-legacy/array-real)<br/>The ARRAY REAL command creates and/or resizes an array of [Real](# "Number between ±1.7e±308 (15 digits)") elements in memory.|
|[**ARRAY TEXT** ( *arrayName* ; *size* {; *size2*} )](../../commands-legacy/array-text)<br/>The ARRAY TEXT command creates and/or resizes an array of [Text](# "A character string that may contain from 0 to 2 GB of text") elements in memory.|
|[**ARRAY TIME** ( *arrayName* ; *size* {; *size2*} )](../../commands-legacy/array-time)<br/>The **ARRAY TIME** command creates and/or resizes an array of Time type elements in memory.|
|[**ARRAY TO LIST** ( *array* ; *list* {; *itemRefs*} )](../../commands-legacy/array-to-list)<br/>The ARRAY TO LIST command creates or replaces the hierarchical list or the choice list (created in the List editor) that is specified in *list* using the elements of the array *array.|
|[**ARRAY TO SELECTION** {( *array* ; *aField* {; *array2* ; *aField2* ; ... ; *arrayN* ; *aFieldN*}{; *} )}](../../commands-legacy/array-to-selection)<br/>The **ARRAY TO SELECTION** command copies one or more arrays into a selection of records.|
|[**BOOLEAN ARRAY FROM SET** ( *booleanArr* {; *set*} )](../../commands-legacy/boolean-array-from-set)<br/>The BOOLEAN ARRAY FROM SET command fills an array of Booleans indicating if each record in the table is or is not in *set*.|
|[**COPY ARRAY** ( *source* ; *destination* )](../../commands-legacy/copy-array)<br/>The COPY ARRAY command creates or overwrites the destination array *destination* with the exact contents, size, and type of the source array *source*.|
|[**Count in array** ( *array* ; *value* ) : Integer](../../commands-legacy/count-in-array)<br/>The Count in array command returns the number of times *value* is found in *array*.|
|[**DELETE FROM ARRAY** ( *array* ; *where* {; *howMany*} )](../../commands-legacy/delete-from-array)<br/>The DELETE FROM ARRAY command deletes one or more elements from *array*.|
|[**DISTINCT ATTRIBUTE PATHS** ( *objectField* ; *pathArray* )](../../commands-legacy/distinct-attribute-paths)<br/>The **DISTINCT ATTRIBUTE PATHS** command returns the list of distinct paths found in the indexed object field you passed in *objectField* for the current selection of the table to which this field belongs.|
|[**DISTINCT ATTRIBUTE VALUES** ( *objectField* ; *path* ; *valuesArray* )](../../commands-legacy/distinct-attribute-values)<br/>The **DISTINCT ATTRIBUTE VALUES** command creates and populates the *valuesArray* with non-repeated (unique) values coming from the *path* attribute in the *objectField* field for the current selection of the table to which this field belongs.|
|[**DISTINCT VALUES** ( *aField* ; *array* {; *countArray*} )](../../commands-legacy/distinct-values)<br/>The **DISTINCT VALUES** command creates and populates the array *array* with non-repeated (unique) values coming from the field *aField* for the current selection of the table to which the field belongs and, optionally, returns the number of occurrences of each value in the *countArray* parameter.|
|[**Find in array** ( *array* ; *value* {; *start*} ) : Integer](../../commands-legacy/find-in-array)<br/>The Find in array command returns the number of the first element in *array* that matches *value*.|
|[**Find in sorted array** ( *array* ; *value* ; > or < {; *posFirst* {; *posLast*}} ) : Boolean](../../commands-legacy/find-in-sorted-array)<br/>The **Find in sorted array** command returns **true** if at least one element in the sorted *array* matches the *value*, and optionally returns position(s) of matched element(s).|
|[**INSERT IN ARRAY** ( *array* ; *where* {; *howMany*} )](../../commands-legacy/insert-in-array)<br/>The INSERT IN ARRAY command inserts one or more elements into the array *array*.|
|[**LIST TO ARRAY** ( *list* ; *array* {; *itemRefs*} )](../../commands-legacy/list-to-array)<br/>The LIST TO ARRAY command creates or overrides the array *array* with the first level items of the list or choice list designated by *list*.|
|[**LONGINT ARRAY FROM SELECTION** ( *aTable* ; *recordArray* {; *selection*} )](../../commands-legacy/longint-array-from-selection)<br/>The LONGINT ARRAY FROM SELECTION command fills the *recordArray* array with the (absolute) record numbers that are in *selection*.|
|[**MULTI SORT ARRAY** ( *array* {; *sort*}{; *array2* ; *sort2* ; ... ; *arrayN* ; *sortN*} ) <br/>
**MULTI SORT ARRAY** ( *ptrArrayName* ; *sortArrayName* )](../../commands-legacy/multi-sort-array)<br/>The MULTI SORT ARRAY command enables you to carry out a multi-level sort on a set of arrays.|
|[**SELECTION RANGE TO ARRAY** ( *start* ; *end* {; aField | aTable ; *array*} {; aField | aTable2 ; *array2* ; ... ; aField | aTableN ; *arrayN*} )](../../commands-legacy/selection-range-to-array)<br/>**SELECTION RANGE TO ARRAY** creates one or more arrays and copies data from the fields or record numbers from the current selection into the arrays.|
|[**SELECTION TO ARRAY** {( aField | aTable ; *array* {; *aField* ; *array* {; *aField2* ; *array2* ; ... ; *aFieldN* ; *arrayN*}}{; *})}](../../commands-legacy/selection-to-array)<br/>The **SELECTION TO ARRAY** command creates one or more arrays and copies data in the fields or record numbers from the current selection into the arrays.|
|[**Size of array** ( *array* ) : Integer](../../commands-legacy/size-of-array)<br/>The Size of array command returns the number of elements in *array*.|
|[**SORT ARRAY** ( *array* {; *array2* ; ... ; *arrayN*}{; > or <} )](../../commands-legacy/sort-array)<br/>The SORT ARRAY command sorts one or more arrays into ascending or descending order.|
|[**TEXT TO ARRAY** ( *varText* ; *arrText* ; *width* ; *fontName* ; *fontSize* {; *fontStyle* {; *}} )](../../commands-legacy/text-to-array)<br/>The **TEXT TO ARRAY** command transforms a text variable into a text array.|
